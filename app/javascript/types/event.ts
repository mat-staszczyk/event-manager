export default type EventType = {
  id: number,
  event_type: string,
  event_date: Date,
  title: string,
  speaker: string,
  host: string,
  published: string,
  created_at: Date,
  updated_at: Date,
}